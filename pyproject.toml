[project]
name = "cs-voice"
version = "0.1.0"
description = "Voice support agent (Orbio take-home)"
requires-python = ">=3.11"
dependencies = [
    "livekit-agents[deepgram,elevenlabs,openai,silero,turn-detector]~=1.0",
    "python-dotenv~=1.0",
]

[tool.uv]
package = false
